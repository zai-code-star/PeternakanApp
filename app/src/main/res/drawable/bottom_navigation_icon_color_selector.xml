<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Item aktif -->
    <item android:color="@color/kuning" android:state_checked="true" />
    <!-- Item tidak aktif -->
    <item android:color="@color/toska" />
</selector>
